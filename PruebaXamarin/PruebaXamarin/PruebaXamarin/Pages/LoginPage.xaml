<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PruebaXamarin.Pages.LoginPage"
             Title="Xamarin"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">


    <ScrollView Padding="10">
        <StackLayout>
            <Label
                FontSize="Medium"
                Text="Email:">
            </Label>
            <Entry                
                Text="{Binding Login.email, Mode=TwoWay}">
            </Entry>
            <Label
                FontSize="Medium"                
                Text="Password:">
            </Label>
            <Entry     
                IsPassword="True" 
                Text="{Binding Login.password, Mode=TwoWay}">
            </Entry>
            <StackLayout>
                <Button HorizontalOptions="Center" VerticalOptions="Center" Command="{Binding LoginCommand}" Text="Login">
                </Button>
            </StackLayout>
        </StackLayout>
    </ScrollView>


</ContentPage>